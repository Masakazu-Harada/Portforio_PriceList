<h1>顧客情報の詳細</h1>

<nav class="nav justify-content-end">
  <%= link_to "顧客情報の一覧へ戻る", customers_path, class: "nav-link" %>
</nav>

<table class=" table table-hover">
  <tbody>
    <tr>
      <td>
        <%= form_with(model: @customer) do |f| %>
          <div class="form-group mb-3">
            <%= f.label :name, class: "form-label" %>
            <%= f.text_field :name, value: @customer.name, class: "form-control", id: "customer_name" %>
          </div>
          <p>販売ランク設定</p>
          <div class="form-check">
            <%= f.collection_radio_buttons(:rank_id, Rank.all, :id, :name, selected: @customer.rank_id) %>
          </div>
          <div class="form-group mb-3">
            <%= f.label :share, class: "form-label" %>
            <%= f.text_area :share, value: @customer.share, class: "form-control", id: "customer_share" %>
          </div>
          <%= f.submit nil, class: "btn btn-primary mr-1" %>
          <%= link_to "削除する", customer_path(@customer),  data: { turbo_method: :delete, turbo_confirm: "「#{@customer.name}」のレコードを削除します。よろしいでしょうか？" }, class: "btn btn-danger" %>
        <% end %>
      </td>
    </tr>
  </tbody>
</table>
